{
  "name": "rust-helper-bot",
  "version": "0.3.0",
  "private": true,
  "description": "Discord + Rust+ helper bot with multi-team sticky failover, web panel and event alerts.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "check": "node --check src/index.js && node --check src/config.js && node --check src/crypto.js && node --check src/db.js && node --check src/discord.js && node --check src/rust.js && node --check src/commands.js && node --check src/web.js",
    "gen-key": "node -e \"console.log(require('node:crypto').randomBytes(32).toString('base64'))\""
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@liamcottle/rustplus.js": "latest",
    "discord.js": "^14.27.0",
    "dotenv": "^16.6.1",
    "express": "^5.1.0"
  }
}
