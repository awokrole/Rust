{
  "name": "rust-helper-bot",
  "version": "0.6.2",
  "private": true,
  "description": "Discord-only Rust+ helper bot with event tracking, Oil Rig detection, deaths and multi-account routing.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "check": "node --check src/index.js && node --check src/config.js && node --check src/crypto.js && node --check src/db.js && node --check src/discord.js && node --check src/rust.js && node --check src/commands.js && node --check src/pairing.js && node --check src/api.js",
    "gen-key": "node -e \"console.log(require('node:crypto').randomBytes(32).toString('base64'))\"",
    "postinstall": "node scripts/patch-rustplus-proto.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@liamcottle/rustplus.js": "2.5.0",
    "discord.js": "^14.27.0",
    "dotenv": "^16.6.1",
    "express": "^5.1.0"
  }
}
